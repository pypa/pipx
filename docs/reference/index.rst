###########
 Reference
###########

Technical reference for pipx: the command-line interface, environment variables, exit codes, machine-readable output,
and on-disk metadata.

The :doc:`CLI reference <cli>` is generated from the argument parser (``pipx.main.build_parser``), so it always matches
the installed version and covers every command and flag.

.. toctree::
    :maxdepth: 1

    cli
    environment-variables
    exit-codes
    json-output
    metadata
    examples
    programs-to-try
