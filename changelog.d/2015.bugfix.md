Serialize direct-reference specifiers as PEP 508's `name @ url` regardless of the installed `packaging` version.
`packaging` gained the space before `@` in 26.0, so the specifier pipx recorded in `pipx_metadata.json` and handed to
the backend used to depend on which `packaging` happened to be present.
